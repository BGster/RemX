[project]
name = "pm"
version = "0.1.0"
description = "Project-Manager CLI"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12.0",
    "rich>=13.7.0",
    "pydantic>=2.0.0",
    "python-dateutil>=2.8.0",
    "pyyaml>=6.0",
    "sqlite-vec>=0.1.0",
]

[project.scripts]
pm = "pm:app"
